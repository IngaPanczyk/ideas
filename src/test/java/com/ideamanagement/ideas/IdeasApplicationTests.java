package com.ideamanagement.ideas;

import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class IdeasApplicationTests {

	@Test
	void contextLoads() {
	}
	/*@Test
	public void testIdeaList() {
		//Given
		Status status = new Status(1,"implemented");
		Idea idea = new Idea( (long)1, "Tytuł 1", "Opis 1", status);
		Idea idea2 - new Idea( "Tytuł 1", "Opis 1", "Implemented");

		idea.setSta
		task.setTaskFinancialDetails(tfd);
		task2.setTaskFinancialDetails(tfd2);
		TaskList taskList = new TaskList(LISTNAME, "ToDo tasks");
		taskList.getTasks().add(task);
		taskList.getTasks().add(task2);
		task.setTaskList(taskList);
		task2.setTaskList(taskList);

		//When
		taskListDao.save(taskList);
		int id = taskList.getId();

		//Then
		Assert.assertNotEquals(0, id);*/

}
